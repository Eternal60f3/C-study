#include <stdio.h>
#include <string.h>
void main(void)
{
    int count;
    scanf("%d", &count);
    for (int i = 0; i < count;i++)
    {
        char *str1;
        scanf("%s", str1);
        for (int o = 0; o < strlen(str1); o++)
        {
            
        }
    }
}